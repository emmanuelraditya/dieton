import { signUp } from "../../services";

export const getSignUpAsync = (ullName,
    email,
    password,
    calorieSize,
    weight,
    height,
    waistline,
    thigh) => {
        return async (dispatch) => {
            dispatch({})
        }
    }